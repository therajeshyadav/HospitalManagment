// // routes/admin.js
// const express = require("express");
// const protect = require("../middleware/protect");

// const router = express.Router();

// // Only Admins allowed
// router.get("/dashboard", protect(["Administrator"]), (req, res) => {
//   res.send("Welcome Admin");
// });

// module.exports = router;
